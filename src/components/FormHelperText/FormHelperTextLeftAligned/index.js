// import { jsx } from '@emotion/core';
import styled from '@emotion/styled';
import FormHelperText from '../FormHelperText';

const FormHelperTextLeftAligned = styled(FormHelperText)({
  marginLeft: '0px',
});

FormHelperTextLeftAligned.displayName = 'FormHelperTextLeftAligned';

export default FormHelperTextLeftAligned;
