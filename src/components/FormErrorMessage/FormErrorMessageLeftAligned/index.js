// import { jsx } from '@emotion/core';
import styled from '@emotion/styled';
import FormErrorMessage from '../FormErrorMessage';

const FormErrorMessageLeftAligned = styled(FormErrorMessage)({
  marginLeft: '0px',
});

FormErrorMessageLeftAligned.displayName = 'FormErrorMessageLeftAligned';

export default FormErrorMessageLeftAligned;
